<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto">
		<item
			android:id="@+id/menu_lessons_list"
			android:icon="@drawable/ic_list"
			android:title="@string/hint_lessons_list_view"
			app:showAsAction="never" />
		<item
			android:id="@+id/menu_lessons_calendar"
			android:icon="@drawable/ic_calendar"
			android:iconTint="@android:color/white"
			android:title="@string/hint_lessons_calendar_view"
			app:showAsAction="never" />
	<item
		android:id="@+id/menu_lessons_notifications"
		android:title="@string/menu_title_submission_list"
		app:actionLayout="@layout/layout_notification_count"
		app:showAsAction="always" />
</menu>